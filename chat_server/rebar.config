%% -*- mode: erlang;erlang-indent-level: 4;indent-tabs-mode: nil -*-

%% == Erlang Compiler ==

{erl_opts, [debug_info,
  warn_missing_spec,
  {src_dirs, ["src"]}]}.

%% == EDoc ==

%% EDoc options
%% {edoc_opts, []}.

%% == Cleanup ==

{clean_files, ["*.beam"]}.